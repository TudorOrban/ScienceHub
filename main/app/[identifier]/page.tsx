export default function IdentifierPage({
    params,
}: {
    params: { identifier: string; projectName: string };
}) {

    return (
        <div>
            Identifier page!
        </div>
    );
}
