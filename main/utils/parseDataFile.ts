import supabase from "./supabase";

// export async function parseDataFile(filename: string, page: number, limit: number) {
//     const { data, error } = await supabase.storage.from("datasets").download(filename);
    

// }